# {{.AppName}}

Minimal [fxkit](https://github.com/fitan/fxkit) project.

## Run

```bash
make run
```

Visit:

- `GET  /greet/{name}` — sample JSON handler (chi)
- `GET  /docs`         — Scalar API reference
- `GET  /openapi.yaml` — OpenAPI spec
- `GET  /healthz`      — liveness
